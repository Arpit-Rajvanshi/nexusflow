# Dev-only dependencies — not included in production Docker images

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1   # async test client for FastAPI

# Mocking / fixtures
faker==33.1.0

# Code quality
ruff==0.8.4
black==24.10.0
mypy==1.13.0
pre-commit==4.0.1

# Dependency management
pip-tools==7.4.1

# Local dev utilities
python-dotenv==1.0.1

# DB migrations (already in requirements.txt, just making explicit for dev)
alembic==1.14.0

# Benchmarking
locust==2.32.3
